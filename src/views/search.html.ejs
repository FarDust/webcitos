<div class="container">
  <div class="row">
    <div class="col-2"></div>
    <div class="col-8">
      <ul>
        <% publications.forEach(publication => {  %>
        <li><%= publication.title %>: <a href="<%= publicationShowPath(publication) %>">see more</a></li>  
        <% }); %>
        <% publications_item.forEach(publication => {  %>
        <li><%= publication.title %>: <a href="<%= publicationShowPath(publication) %>">see more</a></li>  
        <% }); %>
        <% publications.forEach(publication => {  %>
        <li><%= users.title %>: <a href="<%= publicationShowPath(publication) %>">see more</a></li>  
        <% }); %>
      </ul>
    </div>
    <div class="col-2"></div>
  </div>
</div>